{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Blutkrankheit list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Mortality</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_blutkrankheit_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td> <a href="{{ path('admin_blutkrankheit_show', { 'id': entity.id }) }}">
                     {{ entity.name }}
                </a></td>
                <td>{{ entity.mortality }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_blutkrankheit_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_blutkrankheit_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_blutkrankheit_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
