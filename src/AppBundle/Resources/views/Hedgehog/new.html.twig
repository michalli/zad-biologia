{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hedgehog creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_hedgehog') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
